# Data Model

Every entity Smoory persists, with fields, types, relationships, and rationale. This is the authoritative schema. SwiftData `@Model` definitions should mirror this exactly. When something needs to change, update this document first, then the code.

The data model is hierarchical at the top: **Roles → Goals → Projects → Threads → Todos**, with **People**, **Habits**, **Profile**, **Infrastructure**, and **Capture** as orthogonal layers. **Hema** has its own schema documented in `MEMORY.md`.

---

## Conventions

- All entities have `id: UUID` (primary key) and `createdAt: Date` and `updatedAt: Date`.
- Optional fields are nullable. Required fields are non-null.
- Enums are declared as `Int`-backed for SwiftData stability.
- Relationships are bidirectional where useful, modeled as SwiftData `@Relationship`.
- Soft delete is preferred over hard delete for most entities (`isArchived: Bool`, `archivedAt: Date?`). Hard delete only for capture items the user explicitly trashes.

---

## Role

A facet of the user's life. The user has a small fixed number of these (typically 2–5). Roles are first-class because the user has multiple distinct work contexts (employed + own business + freelance), and Smoory needs to weight, filter, and route items per role.

**Fields:**
- `name: String` — e.g. "Employed at Acme", "Smoory (own business)", "Freelance"
- `slug: String` — short identifier, e.g. "work", "biz", "freelance"
- `details: String` — paragraph the user writes describing the role
- `colorHex: String` — for UI tagging
- `workingHours: WorkingHours?` — optional struct: weekday ranges, e.g. Mon-Fri 9-17 for Employed
- `priorityWeight: Double` — default 1.0, user-tunable; affects feed prioritization
- `isActive: Bool` — pause/resume a role (e.g. paused while on extended leave)
- `createdAt`, `updatedAt`

**Relationships:**
- `goals: [Goal]`
- `projects: [Project]`
- `threads: [Thread]`
- `todos: [Todo]` (todos can also be role-less, free-floating)

**Notes:**
- A user can move items between roles. If a freelance project becomes a full-time job, the user can re-tag the project under a new role.
- `WorkingHours` is its own value type (not a separate entity): a struct of weekday ranges. Stored as JSON in a single SwiftData attribute.

---

## Goal

A long-lived intention. Goals are the lens through which Smoory interprets activity. The user has perhaps 5–15 active goals at any time.

**Fields:**
- `role: Role?` — goals usually belong to a role; allow role-less for personal/cross-cutting goals
- `title: String` — "Read 50 pages/day", "Ship Smoory v1 by July", "Be a more present partner"
- `details: String` — paragraph context
- `goalType: GoalType` — enum: `.tracked`, `.reflective`, `.both`
- `trackedSignal: TrackedSignal?` — present if `goalType` is `.tracked` or `.both`
- `reflectiveCadence: ReflectiveCadence?` — present if `goalType` is `.reflective` or `.both`
- `status: GoalStatus` — enum: `.active`, `.paused`, `.achieved`, `.dropped`
- `targetDate: Date?` — optional target completion
- `lastCheckInAt: Date?` — when the last reflective check-in happened
- `lastNudgedAt: Date?` — for nudge rate-limiting

**Sub-types:**

```
enum GoalType { case tracked, reflective, both }

struct TrackedSignal {
  metric: TrackedMetric  // enum: pages_read, sessions_completed, todos_under_project, etc.
  target: Double         // e.g. 50 (pages per day)
  cadence: Cadence       // .daily, .weekly, .monthly
  unit: String           // "pages", "sessions", "minutes"
}

struct ReflectiveCadence {
  frequency: Cadence  // .weekly, .biweekly, .monthly, .quarterly
  preferredDay: Weekday?  // nullable; default: align with week review (Sunday)
  preferredQuestion: String?  // optional opening question; default generated by Claude
}

enum GoalStatus { case active, paused, achieved, dropped }
```

**Relationships:**
- `projects: [Project]` — projects under this goal
- `habits: [Habit]` — habits supporting this goal

**Behavior notes:**
- Tracked goals are checked daily; if behind schedule, a candidate nudge is generated.
- Reflective goals are checked at the cadence frequency; check-ins surface in the week review or as scheduled feed items.
- When `goalType == .both`, both signals fire: tracking informs nudges, reflection happens on cadence.

---

## Project

A concrete, bounded effort that moves a goal forward. Medium-lived (weeks to months).

**Fields:**
- `role: Role?`
- `parentGoal: Goal?`
- `title: String` — "Smoory v1", "Half-marathon training plan"
- `details: String`
- `status: ProjectStatus` — `.planning, .active, .paused, .completed, .abandoned`
- `targetDate: Date?`
- `completedAt: Date?`

**Relationships:**
- `todos: [Todo]`
- `threads: [Thread]`
- `notes: [CaptureItem]` — captured items pinned to this project

**Notes:**
- A project can exist without a parent goal (one-off effort).
- A todo can exist without a parent project (free-floating).

---

## Thread

A lightweight, often inferred grouping of related activity (emails, replies, todos, draft) around a coherent unit of work. Shorter-lived than a project (days to weeks). Often auto-detected; user can also create manually.

**Fields:**
- `role: Role?`
- `title: String` — Smoory-generated, user-editable: "Quote requests for [topic] — Apr 29"
- `summary: String` — Smoory-generated rolling summary of the thread state
- `status: ThreadStatus` — `.open, .active, .awaiting, .closed`
- `inferred: Bool` — true if Smoory inferred this thread; false if user explicitly created
- `inferenceConfidence: Double?` — set if inferred
- `relatedProject: Project?` — optional link
- `closedAt: Date?`
- `closedReason: String?` — "completed successfully", "abandoned", "auto-closed inactive"

**Relationships:**
- `emails: [EmailReference]` — pointers to email message-IDs (since email bodies live in Apple Mail, not in our DB)
- `todos: [Todo]`
- `people: [Person]` — participants in the thread
- `events: [ThreadEvent]` — log of significant moments in the thread (email arrived, todo created, draft sent, status changed)

**Auto-close rules:**
- Status `.awaiting` for 14 days with no activity → Smoory proposes close
- All linked todos completed AND no email activity for 7 days → Smoory proposes close

---

## Todo

The day-to-day tactical unit. Most numerous entity in the system.

**Fields:**
- `role: Role?`
- `parentProject: Project?`
- `parentThread: Thread?`
- `title: String`
- `notes: String`
- `dueDate: Date?`
- `priority: TodoPriority` — `.low, .normal, .high, .urgent`
- `isCompleted: Bool`
- `completedAt: Date?`
- `deferredFrom: Date?` — original due date if deferred
- `deferralCount: Int` — how many times this todo has been deferred (signal for prioritization)
- `source: TodoSource` — enum tracking origin: `.user_chat, .user_quickadd, .ai_proposal, .email_extraction, .calendar_extraction, .manual`
- `relatedPeople: [Person]` — people referenced (e.g. "call Maria")
- `parentTodo: Todo?` — for subtasks, points to the parent Todo. `nil` for top-level todos.
- `subtasks: [Todo]` — children. Empty for childless todos AND for subtasks themselves (one level of nesting only).
- `isArchived: Bool` — soft-delete flag set by `delete_todo`. Archived todos are filtered out of the surface and out of `get_open_todos`. See `DECISIONS.md` "Soft-delete via isArchived."
- `archivedAt: Date?` — set when `isArchived` flips to true.

**Behavior notes:**
- Smoory can propose, user confirms (tier 1).
- Defer with reason captured for pattern observation in week reviews.
- Subtasks are full Todos with `parentTodo` set. They share the same fields, the same tools, and the same memory side effects as top-level todos.
- Subtasks cannot have their own subtasks. This is enforced at insertion time: a Todo's `parentTodo` must itself have `parentTodo == nil`. The constraint is also reflected in `create_subtask`'s validation logic.
- The parent's "completion fraction" (e.g., `3/5`) is computed from `subtasks.filter { $0.isCompleted }.count` over `subtasks.count` at render time, not stored. The parent's `isCompleted` flag is independent of subtask state — completing all subtasks does NOT auto-complete the parent. This preserves user intent: the user might have non-subtask work to finish before considering the parent done.

---

## Habit

A recurring intention tied to a goal. Light treatment in v1 — habits are essentially goals with simple tracked signals. A more elaborate habit module (streaks, calendars, charts) is post-v1.

**Fields:**
- `role: Role?`
- `parentGoal: Goal?`
- `title: String` — "Read 30min before bed", "Exercise 4x per week"
- `targetCadence: Cadence` — `.daily, .weekly`
- `targetCount: Int` — for weekly cadence: target sessions per week
- `lastLoggedAt: Date?`
- `currentStreak: Int`

**Notes:**
- Logging happens via chat ("logged 25 mins of reading"), via review flows, or by Smoory inferring from context.
- v1: surface habits in week review check-ins, no dedicated UI.

---

## Person

A person Smoory tracks. References a corresponding Apple Contacts record. The richest entity, accumulates the most signal over time.

**Fields:**
- `appleContactId: String?` — link to Apple Contacts; nullable for people not yet in Contacts
- `displayName: String`
- `aliases: [String]` — nicknames, "Mike", "the guy from Acme"
- `primaryEmail: String?`
- `emails: [String]` — all known emails
- `company: String?`
- `jobTitle: String?`
- `roles: [Role]` — which Smoory-roles this person sits under (work colleague, client, friend)
- `relationship: String` — short label: "client", "colleague", "friend", "vendor", "candidate"
- `howWeMet: String?` — short note
- `notes: String` — long-form Smoory-curated notes about the person, paragraph form
- `tags: [String]`
- `tone: ToneProfile?` — see below
- `tonePolicyOverride: ToneOverride?` — user-set override of tone profile if any
- `lastInteractionAt: Date?`
- `interactionCount: Int` — counter, informs whether the person should "graduate" to a tracked record
- `isTracked: Bool` — true once Smoory has accepted this person as worth tracking; false for one-off senders

**Sub-types:**

```
struct ToneProfile {
  registerScore: Double   // -1 (very formal) to +1 (very casual)
  lengthPreference: LengthPref  // .terse, .balanced, .verbose
  greetingStyle: String?  // "Hi Emil" vs "Dear Emil" vs "Emil —"
  signOffStyle: String?
  observations: [String]  // free-form notes Smoory has accumulated
  observationCount: Int   // how many emails the profile is based on
}

enum LengthPref { case terse, balanced, verbose }

struct ToneOverride {
  preferredRegister: Double?
  preferredLength: LengthPref?
  notes: String?
}
```

**Notes:**
- Tone profile only used in drafting once `observationCount >= 5`. Below that, Smoory uses generic default tone.
- People view in v1 is **retrieval-only** — no browsable list. Searched and referenced from chat. Browsable view is post-v1.
- Untracked people (one-off senders) are still searchable but don't count toward tone learning, don't show in any list.

---

## Profile

A single record. The user's hand-written context about themselves.

**Fields (singleton):**
- `body: String` — paragraphs the user writes about themselves, their work, their life patterns
- `quickFacts: [String]` — discrete facts: "I'm a software engineer", "I have a daughter named Mia", "I work mornings best"
- `lastEditedAt: Date`
- `editedBy: ProfileEditedBy` — enum: `.user, .smoory_proposed_user_confirmed`

**Notes:**
- The profile is included in nearly every Claude call as context.
- Smoory can propose edits ("you mentioned you have two kids — should I add that?"), user confirms.
- Length capped at ~4000 characters to stay manageable in context.

---

## Infrastructure

Things the user depends on: services, accounts, subscriptions, hosting, domains, hardware. Used to recognize operational alerts ("your domain is expiring") as relevant.

**Fields:**
- `name: String` — "Hetzner production server", "Stripe (Smoory business)", "Porkbun domains"
- `category: InfraCategory` — enum: `.hosting, .domain, .saas, .payment, .source_control, .email_provider, .other`
- `role: Role?` — which role this belongs to
- `provider: String?` — vendor name
- `notes: String`
- `senderHints: [String]` — email addresses or domains that indicate alerts about this resource (used by triage)

**Notes:**
- Mostly populated during onboarding, then accumulates as Smoory recognizes new services from receipts and alerts.

---

## CaptureItem

The inbox of "stuff that doesn't have a home yet." Articles, files, voice notes, screenshots, things the user typed without context.

**Fields:**
- `kind: CaptureKind` — enum: `.text, .url, .file, .image, .voice_note, .pdf`
- `content: String` — for text/URL; for files, the path
- `filePath: String?` — for file/image/PDF/voice
- `extractedText: String?` — text extracted from PDFs, transcribed from voice
- `source: CaptureSource` — enum: `.chat_dropped, .share_extension, .quick_add, .smoory_inferred`
- `processed: Bool` — has Smoory triaged it yet?
- `triageOutcome: String?` — what Smoory did with it: "noted in memory", "linked to Project X", "proposed as todo", "kept pending"
- `linkedTo: [CaptureLink]` — which entities this capture is now associated with

**Sub-types:**

```
struct CaptureLink {
  entityType: String  // "project", "thread", "person", "goal"
  entityId: UUID
  linkReason: String?
}
```

**Notes:**
- Capture in v1 is limited to chat-drop and quick-add. Share extensions and richer capture are post-v1.

---

## FeedItem

Smoory's surfaced output. Every annotation, suggestion, alert, brief, or review is a feed item.

**Fields:**
- `kind: FeedItemKind` — enum: `.email_annotation, .todo_proposal, .calendar_nudge, .morning_brief, .day_review, .week_review, .alert, .memory_candidate, .goal_candidate, .person_candidate, .thread_proposal, .pattern_observation, .check_in_due`
- `priority: Double` — computed score, drives feed ordering
- `pinned: Bool` — true for alerts that should stay at top until handled
- `headline: String` — the one-line summary shown when collapsed
- `body: String` — the full Jarvis-style reasoning, shown when expanded
- `proposedActions: [ProposedAction]` — actionable cards inside the item
- `relatedEntities: [EntityReference]` — links to things the user might want to navigate to
- `confirmationTier: ConfirmationTier` — `.tier1_quick, .tier2_review, .tier3_dialog`
- `state: FeedItemState` — `.active, .acted_upon, .dismissed, .deferred`
- `dueAt: Date?` — for time-sensitive items
- `actedUponAt: Date?`
- `dismissedAt: Date?`
- `archivedAt: Date?`
- `provenance: FeedItemProvenance?` — info about which loop produced this item, for debugging. Optional in v1 because no FeedItem producers exist yet that can guarantee provenance; Phase 3 producers will populate it.
- `payloadJSON: String?` — kind-specific structured payload (added milestone 3.4). For `.morningBrief`, carries the full `MorningBrief` JSON used by the Feed surface and the widget's structured rendering. Optional, default nil — older kinds leave it nil; readers always handle the nil case.

**Sub-types:**

```
struct ProposedAction {
  toolName: String        // "create_todo", "draft_email", "archive_email"
  parameters: [String:Any]  // serialized as JSON
  confirmationTier: ConfirmationTier
  preview: String         // human-readable preview shown in the card
}

enum ConfirmationTier { case tier1_quick, tier2_review, tier3_dialog }
```

**Notes:**
- Feed items have a long lifecycle: appear, get acted on or dismissed, archive, but stay searchable.
- The "history" of feed items is queryable: "what did Smoory suggest about Maria last week?"

---

## Chat persistence

**Chat messages are not stored in SwiftData.** Phase 1 included a `ChatMessage` `@Model` placeholder; that entity was never written by any code path and was removed in milestone 2.5c. Chat history lives in two places:

- **Visible session history:** `ChatViewModel.turns: [Turn]` — in-memory only, lifetime of the app's chat session.
- **Long-term memory:** hema's `MemoryTurn` table (see `MEMORY.md`). Every user and assistant turn is persisted there with embeddings for semantic retrieval.

Tool-call detail is currently surfaced via the `Used:` subtitle on assistant turns and is not persisted beyond hema's content-text. If finer-grained tool-call audit becomes a need, it would be added to `MemoryTurn.metadata` (currently dropped) or to a new dedicated table — not as a SwiftData entity.

---

## Schedule

User-configured times for rituals. A small set of records, mostly singletons.

**Fields:**
- `kind: ScheduleKind` — `.morning_brief, .day_review, .week_review`
- `time: TimeOfDay` — e.g. 08:00 for morning brief
- `daysOfWeek: [Weekday]` — e.g. Mon-Fri only, or all 7
- `notify: Bool` — whether to send a notification or just place in feed
- `enabled: Bool`

**Schedule vs ScheduledAction.** `Schedule` is the user-facing recurring template ("morning brief at 08:00 weekdays"). `ScheduledAction` is the per-occurrence row that the system creates from a `Schedule` when an instance fires. The two are related but never collapsed — a `Schedule` may produce many `ScheduledAction` rows over its lifetime; conversely a one-off `ScheduledAction` (e.g. a user reminder created from chat) has no `Schedule` parent.

---

## ScheduledAction

Per-occurrence row representing one fire of a scheduled ritual or reminder. Created by `ScheduledActionService` either from a `Schedule` (recurring pattern), from a chat tool (`postpone_scheduled_action` / future `create_scheduled_action`), or from a debug command. The service is the only writer; all other code mutates rows through it.

**Fields:**
- `kind: ScheduledActionKind` — `.morningBrief, .dayReview, .weekReview, .goalNudge, .userReminder` (Int-backed)
- `scheduledFor: Date` — current effective fire time (mutated by postpone / reschedule)
- `originalScheduledFor: Date` — fire time at creation; immutable. Anchors recurring regeneration so deferring one occurrence doesn't drift the recurrence.
- `status: ScheduledActionStatus` — `.pending, .firing, .completed, .deferred, .cancelled, .skipped` (Int-backed)
- `createdAt: Date`
- `createdBySource: ActionSource` — `.system, .userChat` (Int-backed)
- `completedAt: Date?`
- `content: String` — body for `.userReminder` / `.goalNudge`; empty for system kinds whose body is generated at fire time
- `relatedEntityID: UUID?` — optional foreign key (a Goal for goalNudge, a Todo for a reminder about a specific item, etc.) — resolved by the action consumer
- `recurringRuleJSON: String?` — JSON-encoded `RecurringRule` (kind: daily/weekly/weekdays/none, timeOfDay: DateComponents, dayOfWeek: Int?). Nil = one-off.
- `deferralCount: Int` — incremented on every postpone / reschedule
- `deferralHistoryJSON: String` — JSON array of `DeferralEntry { at, fromTime, toTime, reason? }` for pattern observation in 3.6
- `userResponseTimeSeconds: Double?` — seconds from `scheduledFor` to `completedAt`

**Lifecycle.** Service-only mutations. `pending` → `firing` (when `scheduledFor` passes — handled by the foreground polling timer or notification arrival) → `completed` / `skipped` / `cancelled`. `pending` ↔ `pending` via `postpone` / `reschedule` (status briefly `.deferred` is reserved but currently elided — postpone keeps status pending and only mutates `scheduledFor`). On `markCompleted` and `skipThisOccurrence`, if `recurringRule != nil` the service generates the next occurrence anchored on `originalScheduledFor + interval`. `cancel` kills the entire recurring thread (no regeneration); `skipThisOccurrence` skips just this one.

**Notification mirroring.** Every pending row has a corresponding `UNUserNotificationCenter` request keyed by `id.uuidString`. Postpone cancels and re-adds; cancel/skip removes; markCompleted lets the OS clean up after fire.

**App Group snapshot.** On every mutation, `ScheduledActionService` writes the upcoming pending queue (max 7 rows, sorted ascending by `scheduledFor`) to `~/Library/Group Containers/group.com.assistant.smoory.shared/scheduled-actions.json` for the future widget.

---

## RuleAdjustment

A learned preference Smoory has proposed and the user has confirmed. Visible and editable in settings.

**Fields:**
- `kind: RuleKind` — `.auto_archive_sender, .auto_archive_pattern, .priority_boost, .priority_demote, .never_propose`
- `details: String` — human-readable
- `pattern: String` — the criterion (sender pattern, subject regex, etc.)
- `weight: Double` — for priority boost/demote
- `confirmed: Bool` — true if user accepted; false for proposed-but-pending
- `proposedAt: Date`
- `confirmedAt: Date?`

---

## CandidateWrite

The structuring layer's queue of candidate writes pending user review. Produced by `StructuringService` after each chat turn (Haiku call), confirmed/rejected via the Feed surface, dispatched to target entities by `CandidateAcceptor`.

**Fields:**
- `id: UUID`
- `createdAt: Date`
- `type: CandidateType` — enum: `.goal, .project, .todo, .person, .infrastructure, .availability, .toneObservation, .fact`
- `content: String` — the record-ready third-person statement from Haiku
- `editedContent: String` — optional user edit pre-confirm; effective content falls back to `content` when empty
- `confidence: Double` — 0.0–1.0; only candidates ≥0.5 are queued (lower discarded)
- `userPhrase: String` — exact words from the user that triggered extraction
- `expiresAt: Date?` — for time-bounded candidates (availability)
- `status: CandidateStatus` — `.pending, .confirmed, .rejected, .autoApplied`
- `sourceTurnID: UUID?` — link to the hema MemoryTurn that produced it (nullable in v1)
- `sourceSessionID: UUID?` — chat session
- `reviewedAt: Date?` — set when status moves out of `.pending`
- `rejectionReason: String?` — captured at reject time (informs future negative training, not yet used)
- `resultEntityID: UUID?` — the entity created on confirm (Goal, Todo, Person, ...) or written fact id

**Behavior notes:**
- `status == .autoApplied` is reserved for future high-confidence-skip-review behavior; v1 always queues regardless of confidence.
- Acceptance dispatch per type is in `Pipeline/Structuring/CandidateAcceptor.swift`.
- `availability` and `toneObservation` candidates write to hema as facts (with tags `availability` / `tone`) until dedicated entities exist; see `PHASE_3_NOTES.md` and `PHASE_4_NOTES.md`.

---

## Memory schema (preview)

The hema memory layer has its own schema, fully detailed in `MEMORY.md`:
- `CompactMemory` — rolling summaries (daily, weekly, overall)
- `MemoryTurn` — embedded conversational turns with vectors
- `SemanticFact` — discrete learned facts with vectors and structured metadata
- `MemoryProvenance` — why a fact was written

---

## Relationships diagram

```
Role ──────┬─── Goal ──── Habit
           │     │
           │     └─── Project ──── Todo (self-referential: parentTodo / subtasks)
           │              │         │
           │              ├──────── Thread
           │              │         │
           │              │         └─── Email refs (external)
           │              │
           │              └─── CaptureItem
           │
           └─── Person ── ToneProfile
                  │
                  └── (linked to Apple Contacts)

Profile ──── (singleton, referenced by all)
Infrastructure ──── (mostly orthogonal, referenced by triage)
FeedItem ──── (references many)
ChatMessage ──── (feeds Hema)
RuleAdjustment ──── (orthogonal, governs feed behavior)
```

**Implementation note on inverse relationships.** SwiftData requires an explicit inverse back-reference on the child (many) side of every bidirectional relationship. These inverses (e.g. `CaptureItem.pinnedToProject` as the inverse of `Project.notes`, `CaptureItem.attachedToMessage` as the inverse of `ChatMessage.attachments`) are added in code as a framework requirement, not as semantic additions to this schema. The conceptual relationship intent is what this document specifies; the inverse property names follow `parentX` / `pinnedToX` / `attachedToX` conventions and are documented alongside the model definitions in `Smoory/Smoory/Models/`.

---

Read **MEMORY.md** next.
